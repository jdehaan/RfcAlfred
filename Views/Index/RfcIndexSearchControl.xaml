<UserControl x:Class="Alfred.Views.Index.RfcIndexSearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="23" d:DesignWidth="570">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" Grid.Row="0" HorizontalAlignment="Stretch" Name="textBoxSearch" VerticalAlignment="Stretch" />
        <Button Grid.Column="1" Grid.Row="0" Content="Search" HorizontalAlignment="Stretch" Name="buttonSearch" VerticalAlignment="Stretch" Click="buttonSearch_Click" />
    </Grid>
</UserControl>
